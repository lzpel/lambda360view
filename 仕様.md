これはnodejs、ReactのOSSとして公開予定の3Dビューアです。

hexapod.jsを読み込みscreenshot.pngのような3D描画ができれば完成です（エッジが表示されることは必須）。
typescript、react-three-fiberで実装してください。
可能な限り生のthreeや生のjsは扱わずreactとtsの機能を使って実装してください。

make -C examples runでサンプルとしてhexapod.jsを読み込み、3D描画ができることを確認できるようにしてください。

- トップレベルはoss:lambda360viewとしてのライブラリです
    - workspacesとしてexamplesを管理します
    - examplesはnextjsでlambda360viewを読み込み、サンプルとしてhexapod.jsを読み込み、3D描画ができることを確認できるようにしてください。

Claude Opus 4.5で実装しました。一撃で素晴らしいデモを作ってくれた。